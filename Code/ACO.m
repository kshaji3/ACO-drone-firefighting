clear all
close all
clc
%% Problem Preparation

%Create the graph

%Draw the graph
figure
subplot(1, 3, 1)
%% ACO Algorithm

%% Initial Parameters
maxIter = 10;
antNo = 10;
droneNo = 5;
tau0 = zeros(0, droneNo);
tau = zeros(0, droneNo);
eta = zeros(0, droneNo);
for i = 1:1:droneNo
end
    
%% Main Loop of ACO

